# 安全说明

## 绝对不要公开的信息

- Windows、服务器或云平台密码；
- SSH 私钥、API Key、AccessKey、验证码；
- 客户端订阅地址、完整配置文件和认证 Cookie；
- 未脱敏的公网 IPv4/IPv6、实例 ID 和管理后台截图。

若这些信息已在公开位置出现，应先删除或遮挡，再立即轮换对应凭据。仅删除消息不能消除已泄露凭据的风险。

## 远程协助原则

- 优先由设备所有者在本机执行可审阅的命令并返回脱敏结果。
- 确需远程维护时，使用临时、可撤销的公钥授权和独立低权限账号，而不是共享密码。
- 每次操作前明确范围；云账户、计费、删除数据、重装系统和防火墙大范围变更需要单独确认。
- 完成后撤销临时访问、关闭临时端口并核对操作日志。

## 服务暴露

- 默认拒绝入站，只放行必要协议和端口。
- 不直接暴露 RDP、SMB、数据库或管理面板。
- 使用强认证和加密传输，定期更新软件。
- 避免执行未经审查的远程 root 脚本，避免通过明文 HTTP 下发配置。
- 日志中不要记录账号密码、访问令牌或完整订阅链接。

## 问题报告

报告故障时提供操作系统版本、网络类型、错误文本、发生时间和已脱敏的地址前缀即可。不要附带任何可直接登录或连接的秘密信息。
